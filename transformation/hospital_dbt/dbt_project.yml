name: hospital_dbt
version: '1.0.0'

profile: hospital_dbt

model-paths: ["models"]

models:
  hospital_dbt:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts